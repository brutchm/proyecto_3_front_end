/**
 * Interface que define la estructura del resumen del dashboard.
 *
 */
export interface IDashboardSummary {
  totalIncome: number;
  totalExpenses: number;
  netBalance: number;
  transactionCount: number;
}

